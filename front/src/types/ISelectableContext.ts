export interface ISelectableContext {
    question: string | undefined,
    selectableVisitors: string[],
    selectableCards: string[],
    selectableMissions: string[],
    selectableOrganisation: string[],
}